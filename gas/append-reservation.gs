/**
 * このファイルは廃止予定です。新規ブック＋新規 GAS 用の一式は次を使ってください。
 *
 *   gas/nail-reserve-v2.gs        … doPost・行追加・sendLineNotification・convertShort 等
 *   gas/NEW-SHEET-AND-SCRIPT.txt  … 手順（LINE は旧と同じトークンで同じ店舗に届く）
 *
 * デプロイ後は index.html の GAS_URL を新ウェブアプリ URL に差し替えてください。
 */
